<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_bank_statement_form_no_quick_create" model="ir.ui.view">
        <field name="name">account.bank.statement.form.no.quick.create</field>
        <field name="model">account.bank.statement</field>
        <field name="inherit_id" ref="account.view_bank_statement_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='line_ids']/tree" position="attributes">
                <t t-if="not user_has_groups('account.group_account_manager')">
                    <attribute name="create">0</attribute>
                </t>
                <t t-if="user_has_groups('account.group_account_manager')">
                    <attribute name="create">1</attribute>
                </t>
            </xpath>
        </field>
    </record>
</odoo> 